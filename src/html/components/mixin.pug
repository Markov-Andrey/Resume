mixin array(key, val1, val2)
  if !val2
    .key #{key}
    div =>
    .text #{val1}
  else
    .key #{key}
    div =>
    .text #{val1}<br>#{val2}

mixin json(key, val1, val2)
  if !val2
    .key #{key}
    div :
    .text #{val1}
  else
    .key #{key}
    div :
    .text #{val1}<br>#{val2}